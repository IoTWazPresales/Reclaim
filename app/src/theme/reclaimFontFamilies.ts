/** PostScript names from @expo-google-fonts packages — use with fontFamily style / Paper fonts config. */
export const RECLAIM_DISPLAY_FONT = {
  bold: 'SchibstedGrotesk_700Bold',
  extraBold: 'SchibstedGrotesk_800ExtraBold',
} as const;

export const RECLAIM_BODY_FONT = {
  regular: 'HankenGrotesk_400Regular',
  medium: 'HankenGrotesk_500Medium',
  semiBold: 'HankenGrotesk_600SemiBold',
} as const;

export const RECLAIM_MONO_FONT = {
  ios: 'Menlo',
  android: 'monospace',
  default: 'monospace',
} as const;
