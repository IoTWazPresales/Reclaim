/**
 * Wearables module - glance models and projection for watch surfaces.
 * Phase 8: Types and projection only. No platform delivery.
 */

export { projectTrainingNextAction } from './WearablesProjectionService';
export type {
  DomainSnapshot,
  TrainingNextActionCard,
  WearableGlanceModel,
} from './models';
