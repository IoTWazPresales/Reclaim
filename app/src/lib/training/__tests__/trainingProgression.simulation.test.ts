/**
 * Scaffold — multi-set session progression simulation (PR-G).
 */
import { describe, it } from 'vitest';

describe.todo('training progression simulation — full session walk (PR-G)');

describe('trainingProgression simulation — scaffold', () => {
  it.todo('3 exercises × 3 sets: listPendingWorkTargets shrinks as sets are logged');
  it.todo('skip exercise removes all its sets from pending chain');
  it.todo('sessionComplete when all non-skipped sets logged');
});
