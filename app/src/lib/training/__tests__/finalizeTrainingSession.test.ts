/**
 * Scaffold — finalizeTrainingSession shared end path (PR-F/G).
 */
import { describe, it } from 'vitest';

describe.todo('finalizeTrainingSession — alert End and in-app Complete parity (PR-F)');

describe('finalizeTrainingSession — scaffold', () => {
  it.todo('clears training_rest, training_set, training_first intents for session');
  it.todo('sets ended_at and summary on session row');
  it.todo('alert End path and handleComplete both invoke same finalize function');
});
