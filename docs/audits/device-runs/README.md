# Device QA run artifacts

Each session creates a dated folder:

`YYYY-MM-DD_<branch>/`

See **`docs/audits/DEVICE_QA_WORKFLOW.md`** for the full protocol.

Do not commit large PNG batches to git unless intentional — use `.gitignore` locally or attach to PR / Drive.
