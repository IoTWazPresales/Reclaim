# N-0014 account-delete completeness

**Commit target:** this node.  
**AC:** vitest list includes `mood_checkins` and `training_sessions`; items via CASCADE.

## Change

`deleteAllPersonalData` now deletes:

- `mood_checkins` (canonical mood SoT; `mood_entries` still deleted too)
- `training_post_session_checkins`, `training_sessions` (CASCADE `training_session_items`, `training_set_logs`)
- `training_program_days`, `training_program_instances`, `training_profiles`

JSON export includes mood check-ins and training sessions/programs/profiles.

Notification wipe on delete: `clearAllIntents()` + `reconcileNotifications()` — not `cancelAllScheduledNotificationsAsync`.

## CONSTRAINT

`training_events` is append-only in repo SQL (`DELETE USING false`). Client cannot wipe it. Recorded as `PERSONAL_DATA_RLS_BLOCKED_DELETE_TABLES`. Live DB UNKNOWN. No migration this node.

## Validation

- `npm run typecheck` — 0
- `npx vitest run src/lib/__tests__/personalDataTables.test.ts` — 3 passed
- `npm test -- --reporter=verbose` — 125 files / 776 tests
- `npm run audit:training-dual-paths` (Git bash) — 27/27
