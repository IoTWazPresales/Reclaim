# N-0015 HC request-set matches manifest keep-set

Connect-time `HEALTH_CONNECT_DEFAULT_METRICS` now includes `steps` and `active_energy`, matching `withHealthConnectPermissions.js` (`READ_STEPS`, `READ_ACTIVE_CALORIES_BURNED`).

Still **does not** request resting HR, HRV, or total calories.

## Validation

- typecheck 0
- `healthConnectRequestSet.test.ts` pass
- `npm test -- --reporter=verbose` 126 files
- dual-path 27/27 (Git bash)
