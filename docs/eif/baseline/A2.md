# A2 HEAD debug client

**Date:** 2026-09-19  
**Device:** emulator-5554 (`sdk_gphone16k_x86_64`, Medium_Phone_API_36.1)

## Install

1. First `npx expo run:android` **BUILD SUCCESSFUL** (11m 43s) then **INSTALL_FAILED_UPDATE_INCOMPATIBLE** — Play/EAS-signed `1.0.4` / versionCode 8 was already on the AVD.
2. `adb uninstall com.fissioncorporation.reclaim` → Success.
3. Second `npx expo run:android` **BUILD SUCCESSFUL** 38s, APK installed, Expo dev client opened with Metro `http://192.168.101.253:8081`.
4. `adb reverse`: `host-16 tcp:8081 tcp:8081`.

## dumpsys (VERIFIED)

Raw: `docs/eif/baseline/A2_DUMPSYS.txt`

| Field | Value |
|---|---|
| versionName | **1.0.5** |
| versionCode | **15** |
| targetSdk | 36 |
| pkgFlags | **DEBUGGABLE** |
| Metro | attached (`adb reverse` 8081 + MainActivity intent `expo-development-client/?url=http://192.168.101.253:8081`) |

N-0010 acceptance is met on device. Human-class node left **proposed** until operator accept (not auto-completed).
