# Release

Store submission checklists, version notes, and **release-specific** decisions.

- **Build:** Expo app lives in `app/` — see `app/eas.json`, `app/app.config.ts`.
- **Scope matrix:** `docs/memory/derived/reclaim_release_scope.md`.
