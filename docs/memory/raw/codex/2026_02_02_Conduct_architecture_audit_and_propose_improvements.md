You are doing a deep architecture audit of a premium, production-grade React Native app (Expo Dev Client + EAS) using Supabase.\n\nPrimary Goal:\nProduce a full current-state architecture map AND a careful, incremental plan to standardize and improve the entire app architecture — including:\n- Auth/session + onboarding gating\n- Local storage, caching, persistence\n- Sync engine (push/pull, retries/backoff, conflict strategy)\n- Notifications engine (intent store + reconcile, TTL, dedupe, auth gating)\n- Insights engine (generation, storage, feedback, confidence, scheduling)\n- Training system/engine (profiles, baselines, programs, sessions, logging, progression, explainability)\n- Wearables surfaces (Apple Watch + Wear OS): glance cards + actionable items\n\nCore Principle:\nUse “best-practice defaults” as a baseline, BUT do not be constrained by them.\nIf you propose something beyond mainstream patterns, you MUST:\n1) Explain why it’s better (clear benefit)\n2) Identify risk/trade-offs\n3) Provide an incremental adoption path (strangler migration)\n4) Define verification/rollback for each step\n\nCRITICAL Constraints:\n- Do NOT propose a big-bang refactor.\n- The migration must be a “strangler fig” plan: one subsystem at a time, each step independently testable and reversible.\n- Preserve existing behavior unless explicitly stated.\n- Every phase must include:\n  (1) exact files/modules likely affected\n  (2) what interfaces to introduce (names + method signatures)\n  (3) how to verify (logs/tests/manual steps)\n  (4) rollback plan (what to revert)\n- Keep the output extremely concrete: cite specific files/functions.\n\nInvariants to enforce (must be explicit in the target design):\n- Single source of truth for auth session\n- Monotonic flags (e.g., has_onboarded never downgrades)\n- No scattered direct remote writes from UI: user writes must be recoverable and consistent\n- Notifications must be controlled by a reconcile loop (no ad-hoc scheduling in screens)\n- Engines (training/insights) must be deterministic and explainable; add decision traces where appropriate\n- Wearables must use the same domain truths: wearable cards are projections of domain state, not a second business-logic copy\n\nArchitecture Model (baseline reference; you may propose improvements):\n- App Shell / Composition Root (boot, DI, routing, global listeners)\n- Presentation (UI + ViewModels/hooks/state machines)\n- Domain (use-cases + business rules)\n- Data Access (repositories, API clients)\n- Persistence (SQLite/AsyncStorage/SecureStore/cache, migrations)\n- Sync subsystem (push/pull, retries/backoff, conflicts; outbox optional but recommended if it solves real risks)\n- Notifications subsystem (intent store + reconcile + delivery guards)\n- Engines subsystem:\n  - Training Engine\n  - Insights Engine\n- Observability (structured logs, breadcrumbs, metrics, error reporting)\n- Platform services (background tasks, deep links, OS constraints)\n- Wearables subsystem (projection + delivery + surfaces)\n\nIMPORTANT NUANCE:\n- Treat “outbox” as OPTIONAL unless you can demonstrate it reduces real risk in this codebase.\n- If you recommend outbox, explain why: what data loss/inconsistency it prevents here, and what the minimal version is.\n- Prefer minimal viable correctness before adding complexity.\n\nDeliverables:\n\nStep 0) Subsystem Index (MANDATORY FIRST OUTPUT)\nList the top 10–30 most important modules/files, grouped by subsystem:\n- App shell/navigation\n- Auth/session\n- Persistence/storage/cache\n- Sync/background tasks\n- Notifications\n- Insights engine\n- Training engine\n- Wearables (or note explicitly if absent)\nFor each file/module: one-line responsibility and key dependencies.\n\nA) Current-State Architecture Map\n- Module map: folders/files + responsibilities.\n- Dependency map (text): who depends on who (UI -> use-cases -> repos -> persistence).\n- Identify boundary violations (UI calling Supabase directly, engines reaching into UI, etc.).\n- Identify duplicated logic (e.g., scheduling notifications from multiple places).\n\nB) Subsystem Deep Dives (each: “what it does now”, “where it lives”, “risks”)\n1) Auth/session + onboarding gating\n2) Persistence + caching\n3) Sync/background (what triggers sync, where state is tracked)\n4) Notifications (what schedules, what cancels, dedupe, TTL, auth gates)\n5) Insights engine (when generated, where stored, feedback loop, confidence, cadence)\n6) Training engine (data model, deterministic rules, baselines, progression, explainability, tests)\n7) Wearables (Apple Watch + Wear OS):\n   - Desired watch surfaces: glance cards + actionable items (training next action, current stats, reminders)\n   - Identify any existing wearable assumptions or code (likely none) and how the current architecture would support it\n\nC) Top Risks (at least 15)\nEach risk must include:\n- Evidence: file/function references\n- Failure mode: race condition, data loss, duplicate notifications, stale cache, inconsistent state, silent failure, etc.\n- Severity + likelihood\n- A concrete mitigation\n\nD) Target Architecture Proposal\n- Proposed boundaries and folder layout (conceptual is OK; don’t require renaming everything immediately).\n- Define stable interfaces (TypeScript) for:\n  - AuthSessionService\n  - Repositories (e.g., TrainingRepository, InsightsRepository, UserStateRepository)\n  - SyncEngine (runOncePush, runOncePull, reconcile, retry/backoff strategy)\n  - NotificationsEngine (setIntent, reconcile, cancelByKey, TTL/dedupe rules)\n  - TrainingEngine + InsightsEngine (pure deterministic functions with decisionTrace outputs)\n  - Observability logger (structured events, breadcrumbs)\n  - WearablesProjectionService (produces typed “Glance Models” from domain state)\n  - WearablesDeliveryService (responsible for update triggers & transport; platform-specific adapters)\n- Explicitly state “Source of Truth” per data type (local DB vs derived cache vs remote).\n- Explicitly state what wearable surfaces should exist and what data each needs.\n\nE) Wearables Target Design (MANDATORY SECTION)\nDesign wearables support in a way that does NOT duplicate business logic:\n- Apple Watch (watchOS): complication/widget-style glance + optional watch app flows + rich notifications\n- Wear OS: tiles/complications + optional watch app flows + rich notifications\n- Strategy:\n  1) Glance Models: small typed models representing cards (e.g., RecoverySummaryCard, TrainingNextActionCard)\n  2) Projection: how to derive these from the domain state deterministically\n  3) Delivery: how/when to refresh surfaces (sync completion, local changes, periodic refresh) with OS constraints\n  4) Offline behavior and caching\n  5) Verification: logging + manual test checklist per surface\nAlso recommend which single surface to build FIRST for maximum impact (and why).\n\nF) Migration Plan (phases)\n- Must be incremental and safe.\n- Provide 6–12 phases maximum; each phase small and verifiable.\n- Preferred order (unless you justify a better one):\n  1) Observability foundation (so later changes are provable)\n  2) Auth truth + monotonic onboarding\n  3) Persistence + repositories (introduce seams)\n  4) Sync correctness (minimal viable push/pull; outbox only if justified)\n  5) Notification intent + reconcile\n  6) Insights engine boundary cleanup + determinism + traces\n  7) Training engine boundary cleanup + determinism + traces\n  8) Wearables Phase 1: Glance Models + projection + one surface prototype\n  9) Wearables Phase 2: expand cards/actions + reliability hardening\n- Each phase includes: patch scope, verification checklist, rollback plan.\n\nG) “Definition of Done”\n- A checklist covering: invariants, tests, logging, offline behavior, sync correctness, notification behavior, engine determinism, wearable refresh correctness, and performance.\n\nStart scanning from these entry points (expand as needed):\n- App.tsx, RootNavigator.tsx, navigation/*\n- Auth flow: AuthScreen.tsx, callback handler, session listeners\n- storage/persistence modules (SQLite/AsyncStorage/SecureStore)\n- sync/background tasks modules\n- notifications modules\n- insights engine modules + insight rendering\n- training system modules (setup, engine, session building, logs)\n- any platform/native integration modules\n\nOutput format:\n- Use headings matching the Deliverables.\n- Use bullet lists with explicit file/function references.\n- Keep recommendations concrete and implementable in phases.\n
Copy
Summary

Added a comprehensive architecture audit report covering subsystem index, current-state map, risks, target architecture, wearables design, and phased migration plan in docs/ARCHITECTURE_AUDIT.md.

Testing

Not run (documentation only).


File (1)
You are a “Cursor Prompt Compiler”.\n\nInput: Use this Architecture Audit you just did. Your job is to convert it into a set of Cursor prompts that implement the plan safely. 
\nHard requirements:\n- Output ONE Cursor prompt per phase (Phase 1..N).\n- Each Cursor prompt must be SPEC-DRIVEN and SURGICAL: minimal diffs, no refactors, no renames, no moving files unless required.\n- Each prompt must include:\n  (1) Goal (1–2 sentences)\n  (2) EXACT files to touch (explicit paths; if uncertain, list candidates and a deterministic way to confirm)\n  (3) Do-not-touch list (files/areas explicitly excluded)\n  (4) Step-by-step edits (what to change and where)\n  (5) Verification checklist (commands + expected signals/log lines)\n  (6) Rollback plan (git commands or “revert these commits/files”)\n  (7) Logging tags to add (e.g. [SYNC_ENGINE], [NOTIF_RECON], [AUTH_TRUTH]) so we can trace behavior\n  (8) Stop conditions (when to stop and report instead of guessing)\n\n- For each phase, choose the best Cursor Agent model and mode based on objective criteria:\n  - Planning/architecture reasoning → choose a “deep reasoning” model.\n  - Precise multi-file code edits + tests → choose a “coding-agent” model.\n  - Small mechanical changes → choose a fast general model.\n  Provide a 1–2 line justification per phase describing WHY that model is the right fit.\n\n- The model choices must ONLY use models that commonly appear in Cursor’s Cloud Agent settings. If you are unsure, provide a primary and fallback model.\n\nOutput format (MANDATORY):\nReturn a single JSON object:\n{\n  "global_assumptions": [...],\n  "global_invariants": [...],\n  "phases": [\n    {\n      "phase": 1,\n      "name": "...",\n      "cursor_mode": "Plan" or "Agent",\n      "cursor_model_primary": "...",\n      "cursor_model_fallback": "...",\n      "why_model": "...",\n      "cursor_prompt": "PASTE-READY PROMPT STRING",\n      "verification": ["..."],\n      "rollback": ["..."],\n      "risk_notes": ["..."]\n    }\n  ]\n}\n\nImportant: DO NOT add extra commentary outside JSON. Do not be vague. If a file list can’t be exact, include a deterministic “how to find it” (ripgrep query, symbol search).\n
Copy
Summary

Replaced the architecture audit document with a JSON-formatted set of Cursor prompts covering each phase, including goals, file scopes, steps, verification, rollback, logging tags, and stop conditions.

Testing

Not run (documentation only).


File (1)
Take the JSON and output a revised JSON plan with these changes:\n\n1) Replace model names with capability roles:\n- best_coding_agent_available\n- best_reasoning_model_available\n- fast_general_model_available\nAlso set cursor_mode per phase appropriately (Plan only where no code changes are required).\n\n2) Verification must use REAL commands from app/package.json.\nRead package.json scripts and choose from: tsc, vitest, jest, lint, expo start --dev-client, etc.\nDo not write “if configured” — be definitive.\n\n3) Insert a new Phase 2.5: Storage scoping wrapper\n- introduce a Storage namespace keyed by userId\n- no migrations yet; just additive + new helpers\n\n4) Split Phase 5 into:\n- Phase 5A: IntentStore + dual path (keep existing schedules but ALSO write intents + reconcile)\n- Phase 5B: Remove direct scheduling entrypoints (health triggers, training rest, mood/med) once validated\n\n5) Phase 3 must specify EXACT call sites (file + function) and must not exceed 2 call sites.\n\n6) Rename Phase 4 to “SyncEngine wrapper + policy + gating (no new pull semantics)” unless you actually implement pull cursors.\n\nReturn only revised JSON.\n
Copy
Summary

Updated the Cursor prompt plan JSON to use capability-based model roles, add Phase 2.5 storage scoping, split notification phases, and include concrete verification commands from package scripts.

Testing

Not run (documentation only).


File (1)
You are a Cursor Plan Finalizer.\n\nContext:\nCursor has the full repo. You must update the existing JSON plan (phases + prompts) to incorporate the reviewer’s final safety fixes, and make the plan maximally executable inside Cursor with minimal ambiguity.\n\nInput:\n1) The JSON plan above you just optimized 
2) You may inspect the repo to validate file paths and scripts (package.json).\n\nOutput:\nReturn ONLY the revised JSON plan. No commentary.\n\nHard requirements (must apply to EVERY phase):\nA) Verification must be executable in THIS repo:\n   1) Read `app/package.json` and list the actual scripts available.\n   2) For each phase, set `verification` to REAL commands that exist in the repo.\n   3) If a script like lint/typecheck/test does not exist, replace it with deterministic fallbacks that are valid for this repo:\n      - Typecheck fallback: (cd app && npx tsc --noEmit) only if tsconfig exists\n      - Tests fallback: (cd app && npx vitest run) only if vitest config exists, otherwise use the repo’s test runner\n      - Lint fallback: (cd app && npx eslint .) only if eslint is installed/configured\n   4) Do not say “if configured”. Be definitive. If no valid verification exists, create the minimal one (e.g., add a `typecheck` script) ONLY if it is truly minimal and safe, and note it in the phase.\n\nB) Adjust Phase 1 stop condition:\n   - Change “>5 files” to “>8 files” (or equivalent) to avoid false stops.\n\nC) Phase 2 (Auth truth) must NOT worsen OAuth / deep link behavior:\n   - Add an explicit constraint: do NOT change OAuth callback parsing rules, URL matching, scheme/intent-filter handling, or redirect URI formats in this phase.\n   - Centralize ownership by delegating calls ONLY; keep callback handler code paths identical.\n   - Add explicit verification steps that confirm OAuth sign-in returns to app and session becomes non-null.\n\nD) Phase 2.5 (ScopedStorage) must prove the seam works with ONE safe call site:\n   - Update exactly ONE low-risk preference read/write to use ScopedStorage when userId is available,\n     while preserving existing keys (no migration). Implement “read new scoped key first, else old key; write both (or write scoped only if safe)”.\n   - Include explicit verification.\n\nE) Phase 3 call sites must be repo-validated:\n   - Confirm the specified screens/files exist and the symbols match.\n   - If not, replace the call sites with the correct existing ones discovered via ripgrep and include the exact `rg` queries used.\n   - Still enforce: NO MORE THAN 2 call sites changed.\n\nF) Phase 5.2 (Notification cutover) must include deterministic reconcile equivalence checks:\n   - Add logging that prints: intent count, scheduled count, and a diff summary (added/removed keys).\n   - Add a manual verification procedure:\n     1) capture scheduled notifications snapshot before cutover (count + IDs/keys)\n     2) after cutover, reconcile and compare; counts should match within documented expected deltas\n   - If equivalence can’t be proven, STOP condition must prevent removal of direct scheduling.\n\nG) Cursor prompts must be “paste-ready” and include:\n   - exact file paths (validated)\n   - explicit do-not-touch list\n   - exact commands\n   - logging tags to add\n   - stop conditions that prevent guessing\n\nH) Model selection:\n   - Keep capability-based model labels as already used.\n   - Ensure cursor_mode is Plan only for phases that are purely interface/type additions; otherwise Agent.\n\nProcedure:\n1) Ingest the provided JSON.\n2) Validate scripts and critical file paths in the repo.\n3) Apply the above changes to the JSON plan.\n4) Return the revised JSON only.\n\nNow wait for the JSON input and then output the revised JSON plan only.\n
Copy
Summary

Updated the JSON plan to reflect stricter auth/deep-link constraints, a single scoped storage call site, and notification cutover equivalence checks, along with the adjusted stop condition and definitive verification commands.

Testing

Not run (documentation only).


File (1)
You are updating an existing JSON plan for a repo that will be executed via Cursor Agent.\n\nInput: the json architecture audit you just optomized 
\nTask: Return ONLY the revised JSON plan. No commentary. Preserve the JSON structure, but apply the modifications below.\n\nGlobal changes (apply everywhere):\n1) Replace cursor_model_primary / cursor_model_fallback placeholders:\n   - For phases 1–7:\n     cursor_model_primary = "OpenAI GPT-5.2 Codex"\n     cursor_model_fallback = "OpenAI GPT-5.1 Codex Mini"\n   - For phases 8–9 (cursor_mode=Plan):\n     cursor_model_primary = "OpenAI GPT-5"\n     cursor_model_fallback = "Anthropic Claude 4.5 Sonnet"\n\n2) Add a new Phase 0 at the beginning:\n   - name: "Repo preflight (scripts + paths)"\n   - cursor_mode: "Agent"\n   - cursor_model_primary: "OpenAI GPT-5.2 Codex"\n   - cursor_model_fallback: "OpenAI GPT-5.1 Codex Mini"\n   - cursor_prompt MUST:\n     a) Read app/package.json and list scripts that actually exist (lint, typecheck, test).\n     b) Define definitive verification commands used in later phases.\n     c) Verify existence of all phase file paths with ls (or equivalent).\n     d) If scripts are missing, set fallbacks:\n        - typecheck fallback: (cd app && npx tsc --noEmit) ONLY if tsconfig exists.\n        - test fallback: (cd app && npx vitest run) ONLY if vitest config exists; else use what exists.\n        - lint fallback: (cd app && npx eslint .) ONLY if eslint exists/configured.\n     e) Output in logs which commands will be used going forward (as [PREFLIGHT]).\n   - verification array must contain real commands determined by the repo. If nothing exists, the phase must STOP and report.\n   - stop condition: stop if cannot find a reliable typecheck command.\n\n3) Update EVERY phase’s verification array and the “Verification checklist” text inside cursor_prompt:\n   - Replace “npm --prefix app run lint/typecheck/test” with the REAL commands chosen in Phase 0.\n   - Do NOT use “if configured”. Be definitive.\n\nPhase-specific changes:\n4) Phase 2 (Auth truth) hard constraint:\n   - Add a bold constraint in cursor_prompt that OAuth callback parsing, URL matching, scheme/intent-filter handling, and redirect URI formats MUST NOT change.\n   - Add a verification step that explicitly checks:\n     - OAuth sign-in returns to app (openAuthSessionAsync resolves)\n     - session becomes non-null\n     - logs show [AUTH_TRUTH] after callback\n   - If those can’t be verified, STOP.\n\n5) Phase 2.5 (ScopedStorage) must include exactly ONE safe call site:\n   - Implement: read scoped first else legacy; write both.\n   - The call site must be explicit (choose one low-risk setting in userSettings).\n   - Add verification: toggle that setting and log [STORAGE_SCOPE] showing read/write used userId scope.\n\n6) Phase 3 call sites MUST be validated via ripgrep:\n   - Modify the phase 3 cursor_prompt so the FIRST steps are:\n     - rg -n "MeditationScreen|listMeditations" app/src/screens\n     - rg -n "MoodScreen|listMood|listMoodCheckins" app/src/screens\n   - If the specific files named do not exist, replace them in the JSON with the correct discovered files and include the exact rg queries used.\n   - Still enforce: no more than 2 call sites.\n\n7) Phase 5.2 equivalence must be deterministic:\n   - Ensure cursor_prompt adds logging that prints:\n     intent_count, scheduled_count, added_keys[], removed_keys[] (or counts)\n   - Add explicit manual procedure: snapshot before/after; compare.\n   - STOP condition must prevent removing direct scheduling if equivalence not proven.\n\nOutput:\nReturn ONLY the revised JSON plan. No markdown, no explanation.\n
Copy
{"global_assumptions":["Repo root is /workspace/Reclaim with app code under app/ and docs under docs/.","Supabase is the backend; Expo (Dev Client + EAS) is used for builds.","No big-bang refactors; each phase is strangler-style and independently reversible.","Existing behavior must be preserved unless explicitly stated in a phase’s steps."],"global_invariants":["Single source of truth for auth session.","Onboarding flag is monotonic (never downgrades).","UI must not write directly to Supabase (all writes go through repositories/outbox if adopted).","Notifications are controlled by a reconcile loop (no ad-hoc scheduling in screens).","Training/Insights engines are deterministic and explainable with decision traces.","Wearable surfaces are projections of domain state, not duplicated business logic."],"phases":[{"phase":0,"name":"Repo preflight (scripts + paths)","cursor_mode":"Agent","cursor_model_primary":"OpenAI GPT-5.2 Codex","cursor_model_fallback":"OpenAI GPT-5.1 Codex Mini","why_model":"Preflight requires safe multi-file inspection and command selection; a coding agent minimizes mistakes.","cursor_prompt":"Goal: Determine the definitive lint/typecheck/test commands and verify all phase file paths exist.\n\nExact files to touch:\n- None (read-only)\n\nDo-not-touch list:\n- Entire repo (read-only phase)\n\nStep-by-step edits:\n1) Read app/package.json and list scripts that actually exist: lint, typecheck, test.\n2) Verify all phase file paths exist using ls (or equivalent) for each path in the plan.\n3) If scripts are missing, set fallbacks:\n - Typecheck fallback: (cd app && npx tsc --noEmit) ONLY if app/tsconfig.json exists.\n - Test fallback: (cd app && npx vitest run) ONLY if app/vitest.config.ts exists; else use what exists.\n - Lint fallback: (cd app && npx eslint .) ONLY if eslint is installed/configured (app/eslint.config.js exists).\n4) Output in logs which commands will be used going forward with [PREFLIGHT] tags.\n\nVerification checklist:\n- Run: cat app/package.json\n- Run: ls app/tsconfig.json app/vitest.config.ts app/eslint.config.js\nExpected: scripts resolved and [PREFLIGHT] logs list definitive commands used later.\n\nRollback plan:\n- None (read-only).\n\nLogging tags to add:\n- [PREFLIGHT]\n\nStop conditions:\n- STOP if a reliable typecheck command cannot be determined.\n","verification":["cat app/package.json","ls app/tsconfig.json app/vitest.config.ts app/eslint.config.js"],"rollback":["None (read-only phase)"],"risk_notes":["No code changes; preflight only."]},{"phase":1,"name":"Observability foundation","cursor_mode":"Agent","cursor_model_primary":"OpenAI GPT-5.2 Codex","cursor_model_fallback":"OpenAI GPT-5.1 Codex Mini","why_model":"This phase involves precise edits across logging/telemetry surfaces with minimal risk; a coding agent is ideal. Fallback to a fast general model if needed.","cursor_prompt":"Goal: Introduce a thin ObservabilityLogger adapter and add structured tags for key flows without refactoring behavior.\n\nExact files to touch:\n- app/src/lib/logger.ts\n- app/src/lib/telemetry.ts\n- app/src/lib/backgroundSync.ts\n- app/src/hooks/useNotifications.ts\n- app/App.tsx\n\nDo-not-touch list:\n- app/src/lib/supabase.ts\n- app/src/lib/api.ts\n- app/src/lib/training/** (no training changes in this phase)\n\nStep-by-step edits:\n1) In app/src/lib/logger.ts, add a lightweight ObservabilityLogger wrapper (or adapter) that prefixes a tag and forwards to existing logger methods (debug/info/warn/error). Keep behavior identical.\n2) In app/src/lib/telemetry.ts, add optional tag parameter support (e.g., tags array or tag string) but do not change existing call sites except those below.\n3) In app/src/lib/backgroundSync.ts, add structured log tags like [SYNC_ENGINE] for task define/register/run/failure.\n4) In app/src/hooks/useNotifications.ts and app/App.tsx, add log tags such as [NOTIF_RECON], [APP_BOOT] around reconciliation and notification setup.\n5) Ensure no behavior changes: only logging additions.\n\nVerification checklist:\n- Run: npm --prefix app run lint\n- Run: npm --prefix app run typecheck\nExpected: commands complete without errors; logs include tags [APP_BOOT], [SYNC_ENGINE], [NOTIF_RECON] on startup.\n\nRollback plan:\n- git revert <commit_sha>\n- Or restore original logger/telemetry/backgroundSync/useNotifications/App.tsx from git.\n\nLogging tags to add:\n- [APP_BOOT], [SYNC_ENGINE], [NOTIF_RECON]\n\nStop conditions:\n- If logger/telemetry is not used in the app or if adding tags requires refactors across >8 files, stop and report the constraint instead of guessing.\n","verification":["npm --prefix app run lint","npm --prefix app run typecheck"],"rollback":["git revert <commit_sha>","git checkout -- app/src/lib/logger.ts app/src/lib/telemetry.ts app/src/lib/backgroundSync.ts app/src/hooks/useNotifications.ts app/App.tsx"],"risk_notes":["Low risk; logging-only changes."]},{"phase":2,"name":"Auth truth + monotonic onboarding","cursor_mode":"Agent","cursor_model_primary":"OpenAI GPT-5.2 Codex","cursor_model_fallback":"OpenAI GPT-5.1 Codex Mini","why_model":"Precise multi-file edits involving auth/session gating and onboarding flags; a coding agent minimizes regressions while keeping diffs small.","cursor_prompt":"Goal: Centralize auth session handling and ensure onboarding flags are monotonic and written through a single service path without changing OAuth/deep-link behavior.\n\nExact files to touch:\n- app/src/providers/AuthProvider.tsx\n- app/src/routing/RootNavigator.tsx\n- app/src/screens/onboarding/completeOnboarding.ts\n- app/src/state/onboarding.ts\n- app/App.tsx\n- New: app/src/lib/authSessionService.ts\n- New: app/src/lib/onboardingService.ts\n\nDo-not-touch list:\n- app/src/lib/supabase.ts\n- app/src/lib/api.ts (no repository changes yet)\n- app/src/hooks/useNotifications.ts\n- OAuth callback parsing rules, URL matching, scheme/intent-filter handling, redirect URI formats (must remain identical)\n\nStep-by-step edits:\n1) Add AuthSessionService (app/src/lib/authSessionService.ts) that encapsulates getSession, refreshIfNeeded, and setSessionFromDeepLink using existing logic from AuthProvider/App.tsx.\n2) Update AuthProvider to use AuthSessionService for initial load + auth state change handling; keep session state in AuthProvider.\n3) In App.tsx, remove or reduce duplicate deep-link session handling; delegate to AuthSessionService to avoid double-apply while keeping callback parsing identical.\n4) In state/onboarding.ts, ensure setHasOnboarded only ever writes true (monotonic) and ignores false overwrites.\n5) In RootNavigator and completeOnboarding.ts, route onboarding writes through onboardingService; avoid duplicated direct writes.\n\nHard constraint: OAuth callback parsing, URL matching, scheme/intent-filter handling, and redirect URI formats MUST NOT change in this phase.\n\nVerification checklist:\n- Run: npm --prefix app run typecheck\n- Run: npm --prefix app run test\n- Manual: OAuth sign-in returns to app (openAuthSessionAsync resolves), session becomes non-null, and logs show [AUTH_TRUTH] after callback.\nExpected: typecheck/tests pass; logs include [AUTH_TRUTH] and [ONBOARD_MONO]. If OAuth callback behavior cannot be verified, STOP.\n\nRollback plan:\n- git revert <commit_sha>\n- Or restore original AuthProvider, RootNavigator, completeOnboarding, state/onboarding, App.tsx.\n\nLogging tags to add:\n- [AUTH_TRUTH], [ONBOARD_MONO]\n\nStop conditions:\n- If removing duplicate deep-link handling requires large refactors or alters OAuth callback parsing/redirect formats, stop and document current coupling instead of guessing.\n","verification":["npm --prefix app run typecheck","npm --prefix app run test"],"rollback":["git revert <commit_sha>","git checkout -- app/src/providers/AuthProvider.tsx app/src/routing/RootNavigator.tsx app/src/screens/onboarding/completeOnboarding.ts app/src/state/onboarding.ts app/App.tsx"],"risk_notes":["Moderate risk due to auth flow changes; keep diffs minimal."]},{"phase":2.5,"name":"Storage scoping wrapper (userId namespace)","cursor_mode":"Agent","cursor_model_primary":"OpenAI GPT-5.2 Codex","cursor_model_fallback":"OpenAI GPT-5.1 Codex Mini","why_model":"Additive helper layer with one safe call site update; a coding agent ensures correctness.","cursor_prompt":"Goal: Introduce a storage namespace keyed by userId with one safe call site using the seam, without migrating existing data.\n\nExact files to touch:\n- New: app/src/persistence/ScopedStorage.ts\n- app/src/lib/userSettings.ts\n\nDo-not-touch list:\n- app/src/lib/api.ts\n- app/src/lib/supabase.ts\n- app/src/screens/\n\nStep-by-step edits:\n1) Create ScopedStorage with methods getItemScoped(userId, key), setItemScoped(userId, key), removeItemScoped(userId, key) that prefix keys with userId.\n2) Update exactly ONE low-risk preference call site in app/src/lib/userSettings.ts: use notificationChimeEnabled setting. Implement: read scoped key first else legacy; write both scoped and legacy keys.\n3) Do not migrate or rewrite existing keys; add only additive functions and one safe call site using them.\n4) Add [STORAGE_SCOPE] logs showing whether scoped or legacy was read and that both were written.\n\nVerification checklist:\n- Run: npm --prefix app run typecheck\n- Manual: toggle notification chime setting and confirm logs show [STORAGE_SCOPE] read/write using userId scope (scoped read or legacy fallback, then dual-write).\n\nRollback plan:\n- git revert <commit_sha>\n- Remove ScopedStorage and revert helper additions.\n\nLogging tags to add:\n- [STORAGE_SCOPE]\n\nStop conditions:\n- If scoping requires migrating existing AsyncStorage keys or touching more than one call site, stop and document migration steps for a later phase.\n","verification":["npm --prefix app run typecheck"],"rollback":["git revert <commit_sha>","git checkout -- app/src/lib/userSettings.ts"],"risk_notes":["Low risk; additive helpers with one safe call site."]},{"phase":3,"name":"Persistence seams (repositories + storage adapters)","cursor_mode":"Agent","cursor_model_primary":"OpenAI GPT-5.2 Codex","cursor_model_fallback":"OpenAI GPT-5.1 Codex Mini","why_model":"Requires careful multi-file edits and new abstractions without large refactors; a coding agent is best.","cursor_prompt":"Goal: Introduce repository and storage adapters as seams without changing runtime behavior, and update no more than 2 call sites.\n\nExact files to touch:\n- app/src/lib/api.ts (wrap or re-export in repository)\n- app/src/lib/userSettings.ts\n- app/src/lib/notificationPreferences.ts\n- app/src/lib/sleepSettings.ts\n- New: app/src/data/TrainingRepository.ts\n- New: app/src/data/InsightsRepository.ts\n- New: app/src/persistence/StorageAdapter.ts\n- Call site 1: app/src/screens/MeditationScreen.tsx (function: useQuery queryFn listMeditations)\n- Call site 2: app/src/screens/MoodScreen.tsx (function: useQuery queryFn listMood or listMoodCheckins)\n\nDo-not-touch list:\n- app/src/lib/supabase.ts\n- app/src/routing/\n- app/src/providers/\n\nStep-by-step edits:\n1) Validate call sites via ripgrep before any edits:\n - rg -n "MeditationScreen|listMeditations" app/src/screens\n - rg -n "MoodScreen|listMood|listMoodCheckins" app/src/screens\n2) If the specific files above do not exist or symbols differ, update the call site list in this plan to the correct files discovered via rg and include the exact rg queries used.\n3) Create StorageAdapter with AsyncStorage-backed get/set/remove methods (no migrations).\n4) Create TrainingRepository and InsightsRepository interfaces + thin implementations that delegate to existing functions in lib/api.ts.\n5) Update exactly two call sites: the validated Meditation screen and Mood screen queryFns to call repository methods.\n6) Keep API signatures identical; no schema changes.\n\nVerification checklist:\n- Run: npm --prefix app run typecheck\n- Run: npm --prefix app run test\nExpected: no failures; touched screens still load data.\n\nRollback plan:\n- git revert <commit_sha>\n- Or revert new files + the two call site changes.\n\nLogging tags to add:\n- [REPO_SEAM], [PERSIST_ADAPTER]\n\nStop conditions:\n- If repository adaptation requires touching more than two call sites, stop and report.\n","verification":["npm --prefix app run typecheck","npm --prefix app run test"],"rollback":["git revert <commit_sha>","git checkout -- app/src/lib/api.ts app/src/lib/userSettings.ts app/src/lib/notificationPreferences.ts app/src/lib/sleepSettings.ts"],"risk_notes":["Low–moderate risk; keep changes shallow and additive."]},{"phase":4,"name":"SyncEngine wrapper + policy + gating (no new pull semantics)","cursor_mode":"Agent","cursor_model_primary":"OpenAI GPT-5.2 Codex","cursor_model_fallback":"OpenAI GPT-5.1 Codex Mini","why_model":"Involves coordinated changes to background sync and sync entrypoints; a coding agent minimizes regressions.","cursor_prompt":"Goal: Introduce a minimal SyncEngine wrapper with retry/backoff and auth gating while keeping existing sync behavior unchanged.\n\nExact files to touch:\n- app/src/lib/sync.ts\n- app/src/lib/backgroundSync.ts\n- New: app/src/sync/SyncEngine.ts\n\nDo-not-touch list:\n- app/src/lib/training/offlineSync.ts\n- app/src/hooks/useNotifications.ts\n\nStep-by-step edits:\n1) Add SyncEngine with runOncePush/runOncePull/reconcile methods that call existing syncAll/syncHealthData; no new pull cursors.\n2) Add a simple retry/backoff policy (max 2 retries, 500ms) inside SyncEngine without changing sync internals.\n3) Add auth gating in SyncEngine (skip if no session) with clear logs.\n4) Update backgroundSync.ts to call SyncEngine instead of syncHealthData directly, adding [SYNC_ENGINE] logs.\n5) Keep existing exports in sync.ts unchanged for now.\n\nVerification checklist:\n- Run: npm --prefix app run typecheck\n- Manual: trigger background sync; logs show [SYNC_ENGINE] success/failure and no runtime errors.\n\nRollback plan:\n- git revert <commit_sha>\n- Or revert backgroundSync.ts and remove SyncEngine.\n\nLogging tags to add:\n- [SYNC_ENGINE], [SYNC_RETRY], [SYNC_GATE]\n\nStop conditions:\n- If SyncEngine requires altering syncAll or syncHealthData logic, stop and report.\n","verification":["npm --prefix app run typecheck"],"rollback":["git revert <commit_sha>","git checkout -- app/src/lib/backgroundSync.ts app/src/lib/sync.ts"],"risk_notes":["Moderate risk; background tasks are sensitive to changes."]},{"phase":5.1,"name":"Notification IntentStore + dual path (write intents + keep existing scheduling)","cursor_mode":"Agent","cursor_model_primary":"OpenAI GPT-5.2 Codex","cursor_model_fallback":"OpenAI GPT-5.1 Codex Mini","why_model":"Touches multiple notification entrypoints and requires careful diff control; a coding agent is required.","cursor_prompt":"Goal: Add NotificationIntentStore and write intents in parallel with existing scheduling (no behavior removal yet).\n\nExact files to touch:\n- app/src/lib/notifications/NotificationScheduler.ts\n- app/src/hooks/useNotifications.ts\n- app/src/lib/health/notificationTriggers.ts\n- app/src/components/training/TrainingSessionView.tsx\n- New: app/src/lib/notifications/NotificationIntentStore.ts\n\nDo-not-touch list:\n- app/src/routing/\n- app/src/providers/\n- app/src/lib/supabase.ts\n\nStep-by-step edits:\n1) Create NotificationIntentStore (AsyncStorage-backed) with setIntent/getIntents/clearIntent; include TTL/dedupe by logicalKey.\n2) In useNotifications.ts, health/notificationTriggers.ts, and TrainingSessionView.tsx, add intent writes alongside existing scheduleNotificationAsync calls (dual path).\n3) Update NotificationScheduler to optionally read intents for diagnostics only; do not change scheduling behavior yet.\n4) Add [NOTIF_INTENT] and [NOTIF_DUAL] logs around intent writes.\n\nVerification checklist:\n- Run: npm --prefix app run lint\n- Run: npm --prefix app run typecheck\nExpected: notifications still schedule; logs show [NOTIF_INTENT] entries.\n\nRollback plan:\n- git revert <commit_sha>\n- Or restore NotificationScheduler/useNotifications/notificationTriggers/TrainingSessionView.tsx.\n\nLogging tags to add:\n- [NOTIF_INTENT], [NOTIF_DUAL]\n\nStop conditions:\n- If intent writes change scheduling behavior, stop and report.\n","verification":["npm --prefix app run lint","npm --prefix app run typecheck"],"rollback":["git revert <commit_sha>","git checkout -- app/src/lib/notifications/NotificationScheduler.ts app/src/hooks/useNotifications.ts app/src/lib/health/notificationTriggers.ts app/src/components/training/TrainingSessionView.tsx"],"risk_notes":["Medium risk; dual-path data writes."]},{"phase":5.2,"name":"Notification scheduling cutover (remove direct scheduling entrypoints)","cursor_mode":"Agent","cursor_model_primary":"OpenAI GPT-5.2 Codex","cursor_model_fallback":"OpenAI GPT-5.1 Codex Mini","why_model":"Removes direct scheduling entrypoints and relies on intent reconcile; needs careful coding and validation.","cursor_prompt":"Goal: Remove direct scheduling entrypoints once intents are validated; drive scheduling via reconcile loop only with deterministic equivalence checks.\n\nExact files to touch:\n- app/src/lib/notifications/NotificationScheduler.ts\n- app/src/hooks/useNotifications.ts\n- app/src/lib/health/notificationTriggers.ts\n- app/src/components/training/TrainingSessionView.tsx\n- app/src/lib/notifications/NotificationIntentStore.ts\n\nDo-not-touch list:\n- app/src/routing/\n- app/src/providers/\n\nStep-by-step edits:\n1) In NotificationScheduler, build plan from intents and reconcile accordingly.\n2) Add reconcile logging that prints: intent_count, scheduled_count, added_keys[], removed_keys[] (or counts).\n3) In useNotifications.ts, remove direct scheduleNotificationAsync calls for med/mood; replace with intent writes and reconcile triggers.\n4) In health/notificationTriggers.ts and TrainingSessionView.tsx, remove direct scheduleNotificationAsync calls and write intents instead.\n5) Add [NOTIF_RECON] logs for reconcile outcomes and [NOTIF_CUTOVER] logs when direct scheduling is removed.\n\nVerification checklist:\n- Run: npm --prefix app run test\n- Manual equivalence procedure:\n a) Before cutover, capture scheduled notifications snapshot (count + logical keys) via Diagnostics screen or getNotificationDiagnostics logs.\n b) After cutover, run reconcile and capture snapshot again.\n c) Compare intent_count vs scheduled_count and added_keys/removed_keys; counts must match within documented expected deltas.\n\nRollback plan:\n- git revert <commit_sha>\n- Or restore direct scheduling calls in affected files.\n\nLogging tags to add:\n- [NOTIF_RECON], [NOTIF_CUTOVER]\n\nStop conditions:\n- If reconcile equivalence (intent vs scheduled counts/keys) cannot be proven with logs/snapshots, STOP and do not remove direct scheduling.\n","verification":["npm --prefix app run test"],"rollback":["git revert <commit_sha>","git checkout -- app/src/lib/notifications/NotificationScheduler.ts app/src/hooks/useNotifications.ts app/src/lib/health/notificationTriggers.ts app/src/components/training/TrainingSessionView.tsx"],"risk_notes":["High risk; notification behavior is user-visible."]},{"phase":6,"name":"Insights engine boundary cleanup + determinism","cursor_mode":"Agent","cursor_model_primary":"OpenAI GPT-5.2 Codex","cursor_model_fallback":"OpenAI GPT-5.1 Codex Mini","why_model":"Requires careful logic extraction into shared helpers; a coding agent reduces regression risk.","cursor_prompt":"Goal: Consolidate insight selection/rotation policy into a shared helper without changing insight rules.\n\nExact files to touch:\n- app/src/providers/InsightsProvider.tsx\n- app/src/lib/insights/pickInsightForScreen.ts\n- app/src/lib/insights/seenStore.ts\n- app/src/screens/Dashboard.tsx\n- app/src/screens/MoodScreen.tsx\n- app/src/screens/SleepScreen.tsx\n- app/src/screens/MedsScreen.tsx\n\nDo-not-touch list:\n- app/src/lib/insights/InsightEngine.ts\n- app/src/lib/insights/contextBuilder.ts\n\nStep-by-step edits:\n1) Add a shared helper (e.g., selectInsightForScreen) that encapsulates unseen filtering and fallback logic.\n2) Replace per-screen selection logic with the helper; preserve telemetry events and tags.\n3) Ensure seenStore calls are centralized via the helper.\n4) Add [INSIGHT_SELECT] logs when selection occurs.\n\nVerification checklist:\n- Run: npm --prefix app run test\nExpected: insights still render; selection consistent across screens.\n\nRollback plan:\n- git revert <commit_sha>\n- Or restore per-screen selection blocks.\n\nLogging tags to add:\n- [INSIGHT_SELECT]\n\nStop conditions:\n- If helper cannot cover all screen-specific constraints without changing behavior, stop and document exceptions.\n","verification":["npm --prefix app run test"],"rollback":["git revert <commit_sha>","git checkout -- app/src/screens/Dashboard.tsx app/src/screens/MoodScreen.tsx app/src/screens/SleepScreen.tsx app/src/screens/MedsScreen.tsx"],"risk_notes":["Moderate risk; selection logic is UX-visible."]},{"phase":7,"name":"Training engine boundary cleanup + deterministic traces","cursor_mode":"Agent","cursor_model_primary":"OpenAI GPT-5.2 Codex","cursor_model_fallback":"OpenAI GPT-5.1 Codex Mini","why_model":"Multi-file edits involving training runtime and data writes; needs a robust coding agent.","cursor_prompt":"Goal: Move training writes behind a repository + offline queue while preserving runtime behavior and decision traces.\n\nExact files to touch:\n- app/src/components/training/TrainingSessionView.tsx\n- app/src/lib/training/offlineQueue.ts\n- app/src/lib/training/offlineSync.ts\n- app/src/lib/api.ts (only via repository)\n- app/src/data/TrainingRepository.ts (from Phase 3)\n\nDo-not-touch list:\n- app/src/lib/training/engine/\n- app/src/lib/training/programPlanner.ts\n\nStep-by-step edits:\n1) Ensure TrainingRepository exposes create/update/log methods that wrap existing api.ts functions.\n2) Replace direct supabase writes in TrainingSessionView.tsx with TrainingRepository calls.\n3) Keep offline queue behavior the same; ensure writes queue when offline and replay via offlineSync.\n4) Add [TRAINING_WRITE] logs for write attempts and [TRAINING_QUEUE] logs for queued ops.\n\nVerification checklist:\n- Run: npm --prefix app run test\n- Manual: run a training session, log sets, end session; no regressions.\n\nRollback plan:\n- git revert <commit_sha>\n- Or restore TrainingSessionView.tsx direct write blocks.\n\nLogging tags to add:\n- [TRAINING_WRITE], [TRAINING_QUEUE]\n\nStop conditions:\n- If replacing direct writes requires touching more than TrainingSessionView + repository, stop and report.\n","verification":["npm --prefix app run test"],"rollback":["git revert <commit_sha>","git checkout -- app/src/components/training/TrainingSessionView.tsx"],"risk_notes":["High risk; training flows are complex."]},{"phase":8,"name":"Wearables Phase 1 (glance models + projection + one surface)","cursor_mode":"Plan","cursor_model_primary":"OpenAI GPT-5","cursor_model_fallback":"Anthropic Claude 4.5 Sonnet","why_model":"This phase is largely architectural planning and interface definition; a deep reasoning model is most appropriate.","cursor_prompt":"Goal: Add minimal wearable projection plumbing with one glance model (TrainingNextActionCard) without platform-specific implementations yet.\n\nExact files to touch:\n- New: app/src/wearables/WearablesProjectionService.ts\n- New: app/src/wearables/models.ts\n- app/src/lib/training/scheduler.ts (if needed for next action derivation)\n- app/src/lib/userSettings.ts (if a toggle is needed)\n\nDo-not-touch list:\n- app/src/components/** (no UI changes)\n- app/src/routing/\n\nStep-by-step edits:\n1) Define WearableGlanceModel types in wearables/models.ts with TrainingNextActionCard.\n2) Implement WearablesProjectionService that takes a DomainSnapshot and outputs TrainingNextActionCard using existing training data (read-only).\n3) Add logging tags [WEAR_PROJ] when projection runs.\n4) No platform delivery yet; do not add watchOS/Wear OS code.\n\nVerification checklist:\n- Run: npm --prefix app run typecheck\nExpected: no type errors; projection types compile.\n\nRollback plan:\n- git revert <commit_sha>\n- Remove wearables folder.\n\nLogging tags to add:\n- [WEAR_PROJ]\n\nStop conditions:\n- If generating a snapshot requires new DB queries or schema changes, stop and document required inputs.\n","verification":["npm --prefix app run typecheck"],"rollback":["git revert <commit_sha>","rm -rf app/src/wearables"],"risk_notes":["Low risk; additive types and projection only."]},{"phase":9,"name":"Wearables Phase 2 (expand cards + delivery adapters)","cursor_mode":"Plan","cursor_model_primary":"OpenAI GPT-5","cursor_model_fallback":"Anthropic Claude 4.5 Sonnet","why_model":"Requires deeper architectural planning for delivery adapters and OS constraints; a reasoning model is best.","cursor_prompt":"Goal: Expand wearable glance models and outline delivery adapters without duplicating domain logic.\n\nExact files to touch:\n- app/src/wearables/WearablesProjectionService.ts\n- app/src/wearables/models.ts\n- New: app/src/wearables/WearablesDeliveryService.ts (interfaces only)\n\nDo-not-touch list:\n- app/src/screens/\n- app/src/components/\n- app/src/routing/\n\nStep-by-step edits:\n1) Add RecoverySummaryCard and MoodCheckinCard to models.ts.\n2) Extend WearablesProjectionService to generate these from existing domain snapshot fields (no new queries).\n3) Add delivery interface with method stubs for Apple Watch and Wear OS adapters (no native code).\n4) Add [WEAR_PROJ] logs per card type.\n\nVerification checklist:\n- Run: npm --prefix app run typecheck\nExpected: no type errors.\n\nRollback plan:\n- git revert <commit_sha>\n- Restore models and projection to Phase 1 only.\n\nLogging tags to add:\n- [WEAR_PROJ]\n\nStop conditions:\n- If platform-specific implementations are required, stop and document expected adapters separately.\n","verification":["npm --prefix app run typecheck"],"rollback":["git revert <commit_sha>","git checkout -- app/src/wearables/models.ts app/src/wearables/WearablesProjectionService.ts"],"risk_notes":["Low risk; additive projection only."]}]}


File (1)
Update the JSON plan with ONLY these changes:\n\n1) Add a root-level object:\n"resolved_commands": { "lint": null, "typecheck": null, "test": null }\n\n2) Phase 0:\n- Expand detection to allow common config filenames:\n  - ESLint: app/eslint.config.* OR app/.eslintrc.* OR eslint entry in app/package.json deps\n  - Vitest: app/vitest.config.* OR app/vite.config.* OR vitest script in app/package.json\n- Add an explicit step that sets resolved_commands.lint/typecheck/test in the JSON plan output (choose the best available commands).\n- Update Phase 0 verification to include: `npm --prefix app run -s lint`, `npm --prefix app run -s typecheck`, `npm --prefix app run -s test` ONLY if those scripts exist; otherwise use the chosen fallbacks.\n\n3) Phases 1–9:\n- Replace hard-coded verification commands with references to resolved_commands (or with the concrete commands Phase 0 selected, substituted directly).\n\n4) Phase 2.5:\n- Replace the hard-coded `notificationChimeEnabled` requirement with:\n  “Select ONE existing boolean user setting already used in the app (validate via rg) and apply scoped read-first-else-legacy; dual-write.”\n\nReturn ONLY the updated JSON.\n
Copy
{"resolved_commands":{"lint":null,"typecheck":null,"test":null},"global_assumptions":["Repo root is /workspace/Reclaim with app code under app/ and docs under docs/.","Supabase is the backend; Expo (Dev Client + EAS) is used for builds.","No big-bang refactors; each phase is strangler-style and independently reversible.","Existing behavior must be preserved unless explicitly stated in a phase’s steps."],"global_invariants":["Single source of truth for auth session.","Onboarding flag is monotonic (never downgrades).","UI must not write directly to Supabase (all writes go through repositories/outbox if adopted).","Notifications are controlled by a reconcile loop (no ad-hoc scheduling in screens).","Training/Insights engines are deterministic and explainable with decision traces.","Wearable surfaces are projections of domain state, not duplicated business logic."],"phases":[{"phase":0,"name":"Repo preflight (scripts + paths)","cursor_mode":"Agent","cursor_model_primary":"OpenAI GPT-5.2 Codex","cursor_model_fallback":"OpenAI GPT-5.1 Codex Mini","why_model":"Preflight requires safe multi-file inspection and command selection; a coding agent minimizes mistakes.","cursor_prompt":"Goal: Determine the definitive lint/typecheck/test commands and verify all phase file paths exist, then set resolved_commands in this JSON plan.\n\nExact files to touch:\n- docs/ARCHITECTURE_AUDIT.md (update resolved_commands only)\n\nDo-not-touch list:\n- Entire repo (read-only beyond updating resolved_commands)\n\nStep-by-step edits:\n1) Read app/package.json and list scripts that actually exist: lint, typecheck, test.\n2) Detect configs: \n - ESLint: app/eslint.config.* OR app/.eslintrc.* OR eslint entry in app/package.json deps.\n - Vitest: app/vitest.config.* OR app/vite.config.* OR vitest script in app/package.json.\n3) Verify all phase file paths exist using ls (or equivalent) for each path in the plan.\n4) Choose definitive commands and set resolved_commands.lint/typecheck/test in docs/ARCHITECTURE_AUDIT.md:\n - Prefer npm --prefix app run -s <script> if script exists.\n - Typecheck fallback: (cd app && npx tsc --noEmit) ONLY if app/tsconfig.json exists.\n - Test fallback: (cd app && npx vitest run) ONLY if app/vitest.config.* or app/vite.config.* exists; else use what exists.\n - Lint fallback: (cd app && npx eslint .) ONLY if ESLint config exists or eslint is installed.\n5) Output in logs which commands will be used going forward with [PREFLIGHT] tags.\n\nVerification checklist:\n- Run: resolved_commands.lint\n- Run: resolved_commands.typecheck\n- Run: resolved_commands.test\nExpected: resolved_commands are set and [PREFLIGHT] logs list definitive commands used later.\n\nRollback plan:\n- git revert <commit_sha>\n\nLogging tags to add:\n- [PREFLIGHT]\n\nStop conditions:\n- STOP if a reliable typecheck command cannot be determined.\n","verification":["resolved_commands.lint","resolved_commands.typecheck","resolved_commands.test"],"rollback":["git revert <commit_sha>"],"risk_notes":["Minimal change; only resolved_commands update."]},{"phase":1,"name":"Observability foundation","cursor_mode":"Agent","cursor_model_primary":"OpenAI GPT-5.2 Codex","cursor_model_fallback":"OpenAI GPT-5.1 Codex Mini","why_model":"This phase involves precise edits across logging/telemetry surfaces with minimal risk; a coding agent is ideal. Fallback to a fast general model if needed.","cursor_prompt":"Goal: Introduce a thin ObservabilityLogger adapter and add structured tags for key flows without refactoring behavior.\n\nExact files to touch:\n- app/src/lib/logger.ts\n- app/src/lib/telemetry.ts\n- app/src/lib/backgroundSync.ts\n- app/src/hooks/useNotifications.ts\n- app/App.tsx\n\nDo-not-touch list:\n- app/src/lib/supabase.ts\n- app/src/lib/api.ts\n- app/src/lib/training/** (no training changes in this phase)\n\nStep-by-step edits:\n1) In app/src/lib/logger.ts, add a lightweight ObservabilityLogger wrapper (or adapter) that prefixes a tag and forwards to existing logger methods (debug/info/warn/error). Keep behavior identical.\n2) In app/src/lib/telemetry.ts, add optional tag parameter support (e.g., tags array or tag string) but do not change existing call sites except those below.\n3) In app/src/lib/backgroundSync.ts, add structured log tags like [SYNC_ENGINE] for task define/register/run/failure.\n4) In app/src/hooks/useNotifications.ts and app/App.tsx, add log tags such as [NOTIF_RECON], [APP_BOOT] around reconciliation and notification setup.\n5) Ensure no behavior changes: only logging additions.\n\nVerification checklist:\n- Run: resolved_commands.lint\n- Run: resolved_commands.typecheck\nExpected: commands complete without errors; logs include tags [APP_BOOT], [SYNC_ENGINE], [NOTIF_RECON] on startup.\n\nRollback plan:\n- git revert <commit_sha>\n- Or restore original logger/telemetry/backgroundSync/useNotifications/App.tsx from git.\n\nLogging tags to add:\n- [APP_BOOT], [SYNC_ENGINE], [NOTIF_RECON]\n\nStop conditions:\n- If logger/telemetry is not used in the app or if adding tags requires refactors across >8 files, stop and report the constraint instead of guessing.\n","verification":["resolved_commands.lint","resolved_commands.typecheck"],"rollback":["git revert <commit_sha>","git checkout -- app/src/lib/logger.ts app/src/lib/telemetry.ts app/src/lib/backgroundSync.ts app/src/hooks/useNotifications.ts app/App.tsx"],"risk_notes":["Low risk; logging-only changes."]},{"phase":2,"name":"Auth truth + monotonic onboarding","cursor_mode":"Agent","cursor_model_primary":"OpenAI GPT-5.2 Codex","cursor_model_fallback":"OpenAI GPT-5.1 Codex Mini","why_model":"Precise multi-file edits involving auth/session gating and onboarding flags; a coding agent minimizes regressions while keeping diffs small.","cursor_prompt":"Goal: Centralize auth session handling and ensure onboarding flags are monotonic and written through a single service path without changing OAuth/deep-link behavior.\n\nExact files to touch:\n- app/src/providers/AuthProvider.tsx\n- app/src/routing/RootNavigator.tsx\n- app/src/screens/onboarding/completeOnboarding.ts\n- app/src/state/onboarding.ts\n- app/App.tsx\n- New: app/src/lib/authSessionService.ts\n- New: app/src/lib/onboardingService.ts\n\nDo-not-touch list:\n- app/src/lib/supabase.ts\n- app/src/lib/api.ts (no repository changes yet)\n- app/src/hooks/useNotifications.ts\n- OAuth callback parsing rules, URL matching, scheme/intent-filter handling, redirect URI formats (must remain identical)\n\nStep-by-step edits:\n1) Add AuthSessionService (app/src/lib/authSessionService.ts) that encapsulates getSession, refreshIfNeeded, and setSessionFromDeepLink using existing logic from AuthProvider/App.tsx.\n2) Update AuthProvider to use AuthSessionService for initial load + auth state change handling; keep session state in AuthProvider.\n3) In app/App.tsx, remove or reduce duplicate deep-link session handling; delegate to AuthSessionService to avoid double-apply while keeping callback parsing identical.\n4) In state/onboarding.ts, ensure setHasOnboarded only ever writes true (monotonic) and ignores false overwrites.\n5) In RootNavigator and completeOnboarding.ts, route onboarding writes through onboardingService; avoid duplicated direct writes.\n\nHard constraint: OAuth callback parsing, URL matching, scheme/intent-filter handling, and redirect URI formats MUST NOT change in this phase.\n\nVerification checklist:\n- Run: resolved_commands.typecheck\n- Run: resolved_commands.test\n- Manual: OAuth sign-in returns to app (openAuthSessionAsync resolves), session becomes non-null, and logs show [AUTH_TRUTH] after callback.\nExpected: typecheck/tests pass; logs include [AUTH_TRUTH] and [ONBOARD_MONO]. If OAuth callback behavior cannot be verified, STOP.\n\nRollback plan:\n- git revert <commit_sha>\n- Or restore original AuthProvider, RootNavigator, completeOnboarding, state/onboarding, App.tsx.\n\nLogging tags to add:\n- [AUTH_TRUTH], [ONBOARD_MONO]\n\nStop conditions:\n- If removing duplicate deep-link handling requires large refactors or alters OAuth callback parsing/redirect formats, stop and document current coupling instead of guessing.\n","verification":["resolved_commands.typecheck","resolved_commands.test"],"rollback":["git revert <commit_sha>","git checkout -- app/src/providers/AuthProvider.tsx app/src/routing/RootNavigator.tsx app/src/screens/onboarding/completeOnboarding.ts app/src/state/onboarding.ts app/App.tsx"],"risk_notes":["Moderate risk due to auth flow changes; keep diffs minimal."]},{"phase":2.5,"name":"Storage scoping wrapper (userId namespace)","cursor_mode":"Agent","cursor_model_primary":"OpenAI GPT-5.2 Codex","cursor_model_fallback":"OpenAI GPT-5.1 Codex Mini","why_model":"Additive helper layer with one safe call site update; a coding agent ensures correctness.","cursor_prompt":"Goal: Introduce a storage namespace keyed by userId with one safe call site using the seam, without migrating existing data.\n\nExact files to touch:\n- New: app/src/persistence/ScopedStorage.ts\n- app/src/lib/userSettings.ts\n\nDo-not-touch list:\n- app/src/lib/api.ts\n- app/src/lib/supabase.ts\n- app/src/screens/\n\nStep-by-step edits:\n1) Create ScopedStorage with methods getItemScoped(userId, key), setItemScoped(userId, key), removeItemScoped(userId, key) that prefix keys with userId.\n2) Select ONE existing boolean user setting already used in the app (validate via rg) and apply: read scoped first else legacy; dual-write (scoped + legacy).\n3) Do not migrate or rewrite existing keys; add only additive functions and one safe call site using them.\n4) Add [STORAGE_SCOPE] logs showing whether scoped or legacy was read and that both were written.\n\nVerification checklist:\n- Run: resolved_commands.typecheck\n- Manual: toggle the selected setting and confirm logs show [STORAGE_SCOPE] read/write using userId scope (scoped read or legacy fallback, then dual-write).\n\nRollback plan:\n- git revert <commit_sha>\n- Remove ScopedStorage and revert helper additions.\n\nLogging tags to add:\n- [STORAGE_SCOPE]\n\nStop conditions:\n- If scoping requires migrating existing AsyncStorage keys or touching more than one call site, stop and document migration steps for a later phase.\n","verification":["resolved_commands.typecheck"],"rollback":["git revert <commit_sha>","git checkout -- app/src/lib/userSettings.ts"],"risk_notes":["Low risk; additive helpers with one safe call site."]},{"phase":3,"name":"Persistence seams (repositories + storage adapters)","cursor_mode":"Agent","cursor_model_primary":"OpenAI GPT-5.2 Codex","cursor_model_fallback":"OpenAI GPT-5.1 Codex Mini","why_model":"Requires careful multi-file edits and new abstractions without large refactors; a coding agent is best.","cursor_prompt":"Goal: Introduce repository and storage adapters as seams without changing runtime behavior, and update no more than 2 call sites.\n\nExact files to touch:\n- app/src/lib/api.ts (wrap or re-export in repository)\n- app/src/lib/userSettings.ts\n- app/src/lib/notificationPreferences.ts\n- app/src/lib/sleepSettings.ts\n- New: app/src/data/TrainingRepository.ts\n- New: app/src/data/InsightsRepository.ts\n- New: app/src/persistence/StorageAdapter.ts\n- Call site 1: app/src/screens/MeditationScreen.tsx (function: useQuery queryFn listMeditations)\n- Call site 2: app/src/screens/MoodScreen.tsx (function: useQuery queryFn listMood or listMoodCheckins)\n\nDo-not-touch list:\n- app/src/lib/supabase.ts\n- app/src/routing/\n- app/src/providers/\n\nStep-by-step edits:\n1) Validate call sites via ripgrep before any edits:\n - rg -n "MeditationScreen|listMeditations" app/src/screens\n - rg -n "MoodScreen|listMood|listMoodCheckins" app/src/screens\n2) If the specific files above do not exist or symbols differ, update the call site list in this plan to the correct files discovered via rg and include the exact rg queries used.\n3) Create StorageAdapter with AsyncStorage-backed get/set/remove methods (no migrations).\n4) Create TrainingRepository and InsightsRepository interfaces + thin implementations that delegate to existing functions in lib/api.ts.\n5) Update exactly two call sites: the validated Meditation screen and Mood screen queryFns to call repository methods.\n6) Keep API signatures identical; no schema changes.\n\nVerification checklist:\n- Run: resolved_commands.typecheck\n- Run: resolved_commands.test\nExpected: no failures; touched screens still load data.\n\nRollback plan:\n- git revert <commit_sha>\n- Or revert new files + the two call site changes.\n\nLogging tags to add:\n- [REPO_SEAM], [PERSIST_ADAPTER]\n\nStop conditions:\n- If repository adaptation requires touching more than two call sites, stop and report.\n","verification":["resolved_commands.typecheck","resolved_commands.test"],"rollback":["git revert <commit_sha>","git checkout -- app/src/lib/api.ts app/src/lib/userSettings.ts app/src/lib/notificationPreferences.ts app/src/lib/sleepSettings.ts"],"risk_notes":["Low–moderate risk; keep changes shallow and additive."]},{"phase":4,"name":"SyncEngine wrapper + policy + gating (no new pull semantics)","cursor_mode":"Agent","cursor_model_primary":"OpenAI GPT-5.2 Codex","cursor_model_fallback":"OpenAI GPT-5.1 Codex Mini","why_model":"Involves coordinated changes to background sync and sync entrypoints; a coding agent minimizes regressions.","cursor_prompt":"Goal: Introduce a minimal SyncEngine wrapper with retry/backoff and auth gating while keeping existing sync behavior unchanged.\n\nExact files to touch:\n- app/src/lib/sync.ts\n- app/src/lib/backgroundSync.ts\n- New: app/src/sync/SyncEngine.ts\n\nDo-not-touch list:\n- app/src/lib/training/offlineSync.ts\n- app/src/hooks/useNotifications.ts\n\nStep-by-step edits:\n1) Add SyncEngine with runOncePush/runOncePull/reconcile methods that call existing syncAll/syncHealthData; no new pull cursors.\n2) Add a simple retry/backoff policy (max 2 retries, 500ms) inside SyncEngine without changing sync internals.\n3) Add auth gating in SyncEngine (skip if no session) with clear logs.\n4) Update backgroundSync.ts to call SyncEngine instead of syncHealthData directly, adding [SYNC_ENGINE] logs.\n5) Keep existing exports in sync.ts unchanged for now.\n\nVerification checklist:\n- Run: resolved_commands.typecheck\n- Manual: trigger background sync; logs show [SYNC_ENGINE] success/failure and no runtime errors.\n\nRollback plan:\n- git revert <commit_sha>\n- Or revert backgroundSync.ts and remove SyncEngine.\n\nLogging tags to add:\n- [SYNC_ENGINE], [SYNC_RETRY], [SYNC_GATE]\n\nStop conditions:\n- If SyncEngine requires altering syncAll or syncHealthData logic, stop and report.\n","verification":["resolved_commands.typecheck"],"rollback":["git revert <commit_sha>","git checkout -- app/src/lib/backgroundSync.ts app/src/lib/sync.ts"],"risk_notes":["Moderate risk; background tasks are sensitive to changes."]},{"phase":5.1,"name":"Notification IntentStore + dual path (write intents + keep existing scheduling)","cursor_mode":"Agent","cursor_model_primary":"OpenAI GPT-5.2 Codex","cursor_model_fallback":"OpenAI GPT-5.1 Codex Mini","why_model":"Touches multiple notification entrypoints and requires careful diff control; a coding agent is required.","cursor_prompt":"Goal: Add NotificationIntentStore and write intents in parallel with existing scheduling (no behavior removal yet).\n\nExact files to touch:\n- app/src/lib/notifications/NotificationScheduler.ts\n- app/src/hooks/useNotifications.ts\n- app/src/lib/health/notificationTriggers.ts\n- app/src/components/training/TrainingSessionView.tsx\n- New: app/src/lib/notifications/NotificationIntentStore.ts\n\nDo-not-touch list:\n- app/src/routing/\n- app/src/providers/\n- app/src/lib/supabase.ts\n\nStep-by-step edits:\n1) Create NotificationIntentStore (AsyncStorage-backed) with setIntent/getIntents/clearIntent; include TTL/dedupe by logicalKey.\n2) In useNotifications.ts, health/notificationTriggers.ts, and TrainingSessionView.tsx, add intent writes alongside existing scheduleNotificationAsync calls (dual path).\n3) Update NotificationScheduler to optionally read intents for diagnostics only; do not change scheduling behavior yet.\n4) Add [NOTIF_INTENT] and [NOTIF_DUAL] logs around intent writes.\n\nVerification checklist:\n- Run: resolved_commands.lint\n- Run: resolved_commands.typecheck\nExpected: notifications still schedule; logs show [NOTIF_INTENT] entries.\n\nRollback plan:\n- git revert <commit_sha>\n- Or restore NotificationScheduler/useNotifications/notificationTriggers/TrainingSessionView.tsx.\n\nLogging tags to add:\n- [NOTIF_INTENT], [NOTIF_DUAL]\n\nStop conditions:\n- If intent writes change scheduling behavior, stop and report.\n","verification":["resolved_commands.lint","resolved_commands.typecheck"],"rollback":["git revert <commit_sha>","git checkout -- app/src/lib/notifications/NotificationScheduler.ts app/src/hooks/useNotifications.ts app/src/lib/health/notificationTriggers.ts app/src/components/training/TrainingSessionView.tsx"],"risk_notes":["Medium risk; dual-path data writes."]},{"phase":5.2,"name":"Notification scheduling cutover (remove direct scheduling entrypoints)","cursor_mode":"Agent","cursor_model_primary":"OpenAI GPT-5.2 Codex","cursor_model_fallback":"OpenAI GPT-5.1 Codex Mini","why_model":"Removes direct scheduling entrypoints and relies on intent reconcile; needs careful coding and validation.","cursor_prompt":"Goal: Remove direct scheduling entrypoints once intents are validated; drive scheduling via reconcile loop only with deterministic equivalence checks.\n\nExact files to touch:\n- app/src/lib/notifications/NotificationScheduler.ts\n- app/src/hooks/useNotifications.ts\n- app/src/lib/health/notificationTriggers.ts\n- app/src/components/training/TrainingSessionView.tsx\n- app/src/lib/notifications/NotificationIntentStore.ts\n\nDo-not-touch list:\n- app/src/routing/\n- app/src/providers/\n\nStep-by-step edits:\n1) In NotificationScheduler, build plan from intents and reconcile accordingly.\n2) Add reconcile logging that prints: intent_count, scheduled_count, added_keys[], removed_keys[] (or counts).\n3) In useNotifications.ts, remove direct scheduleNotificationAsync calls for med/mood; replace with intent writes and reconcile triggers.\n4) In health/notificationTriggers.ts and TrainingSessionView.tsx, remove direct scheduleNotificationAsync calls and write intents instead.\n5) Add [NOTIF_RECON] logs for reconcile outcomes and [NOTIF_CUTOVER] logs when direct scheduling is removed.\n\nVerification checklist:\n- Run: resolved_commands.test\n- Manual equivalence procedure:\n a) Before cutover, capture scheduled notifications snapshot (count + logical keys) via Diagnostics screen or getNotificationDiagnostics logs.\n b) After cutover, run reconcile and capture snapshot again.\n c) Compare intent_count vs scheduled_count and added_keys/removed_keys; counts must match within documented expected deltas.\n\nRollback plan:\n- git revert <commit_sha>\n- Or restore direct scheduling calls in affected files.\n\nLogging tags to add:\n- [NOTIF_RECON], [NOTIF_CUTOVER]\n\nStop conditions:\n- If reconcile equivalence (intent vs scheduled counts/keys) cannot be proven with logs/snapshots, STOP and do not remove direct scheduling.\n","verification":["resolved_commands.test"],"rollback":["git revert <commit_sha>","git checkout -- app/src/lib/notifications/NotificationScheduler.ts app/src/hooks/useNotifications.ts app/src/lib/health/notificationTriggers.ts app/src/components/training/TrainingSessionView.tsx"],"risk_notes":["High risk; notification behavior is user-visible."]},{"phase":6,"name":"Insights engine boundary cleanup + determinism","cursor_mode":"Agent","cursor_model_primary":"OpenAI GPT-5.2 Codex","cursor_model_fallback":"OpenAI GPT-5.1 Codex Mini","why_model":"Requires careful logic extraction into shared helpers; a coding agent reduces regression risk.","cursor_prompt":"Goal: Consolidate insight selection/rotation policy into a shared helper without changing insight rules.\n\nExact files to touch:\n- app/src/providers/InsightsProvider.tsx\n- app/src/lib/insights/pickInsightForScreen.ts\n- app/src/lib/insights/seenStore.ts\n- app/src/screens/Dashboard.tsx\n- app/src/screens/MoodScreen.tsx\n- app/src/screens/SleepScreen.tsx\n- app/src/screens/MedsScreen.tsx\n\nDo-not-touch list:\n- app/src/lib/insights/InsightEngine.ts\n- app/src/lib/insights/contextBuilder.ts\n\nStep-by-step edits:\n1) Add a shared helper (e.g., selectInsightForScreen) that encapsulates unseen filtering and fallback logic.\n2) Replace per-screen selection logic with the helper; preserve telemetry events and tags.\n3) Ensure seenStore calls are centralized via the helper.\n4) Add [INSIGHT_SELECT] logs when selection occurs.\n\nVerification checklist:\n- Run: resolved_commands.test\nExpected: insights still render; selection consistent across screens.\n\nRollback plan:\n- git revert <commit_sha>\n- Or restore per-screen selection blocks.\n\nLogging tags to add:\n- [INSIGHT_SELECT]\n\nStop conditions:\n- If helper cannot cover all screen-specific constraints without changing behavior, stop and document exceptions.\n","verification":["resolved_commands.test"],"rollback":["git revert <commit_sha>","git checkout -- app/src/screens/Dashboard.tsx app/src/screens/MoodScreen.tsx app/src/screens/SleepScreen.tsx app/src/screens/MedsScreen.tsx"],"risk_notes":["Moderate risk; selection logic is UX-visible."]},{"phase":7,"name":"Training engine boundary cleanup + deterministic traces","cursor_mode":"Agent","cursor_model_primary":"OpenAI GPT-5.2 Codex","cursor_model_fallback":"OpenAI GPT-5.1 Codex Mini","why_model":"Multi-file edits involving training runtime and data writes; needs a robust coding agent.","cursor_prompt":"Goal: Move training writes behind a repository + offline queue while preserving runtime behavior and decision traces.\n\nExact files to touch:\n- app/src/components/training/TrainingSessionView.tsx\n- app/src/lib/training/offlineQueue.ts\n- app/src/lib/training/offlineSync.ts\n- app/src/lib/api.ts (only via repository)\n- app/src/data/TrainingRepository.ts (from Phase 3)\n\nDo-not-touch list:\n- app/src/lib/training/engine/\n- app/src/lib/training/programPlanner.ts\n\nStep-by-step edits:\n1) Ensure TrainingRepository exposes create/update/log methods that wrap existing api.ts functions.\n2) Replace direct supabase writes in TrainingSessionView.tsx with TrainingRepository calls.\n3) Keep offline queue behavior the same; ensure writes queue when offline and replay via offlineSync.\n4) Add [TRAINING_WRITE] logs for write attempts and [TRAINING_QUEUE] logs for queued ops.\n\nVerification checklist:\n- Run: resolved_commands.test\n- Manual: run a training session, log sets, end session; no regressions.\n\nRollback plan:\n- git revert <commit_sha>\n- Or restore TrainingSessionView.tsx direct write blocks.\n\nLogging tags to add:\n- [TRAINING_WRITE], [TRAINING_QUEUE]\n\nStop conditions:\n- If replacing direct writes requires touching more than TrainingSessionView + repository, stop and report.\n","verification":["resolved_commands.test"],"rollback":["git revert <commit_sha>","git checkout -- app/src/components/training/TrainingSessionView.tsx"],"risk_notes":["High risk; training flows are complex."]},{"phase":8,"name":"Wearables Phase 1 (glance models + projection + one surface)","cursor_mode":"Plan","cursor_model_primary":"OpenAI GPT-5","cursor_model_fallback":"Anthropic Claude 4.5 Sonnet","why_model":"This phase is largely architectural planning and interface definition; a deep reasoning model is most appropriate.","cursor_prompt":"Goal: Add minimal wearable projection plumbing with one glance model (TrainingNextActionCard) without platform-specific implementations yet.\n\nExact files to touch:\n- New: app/src/wearables/WearablesProjectionService.ts\n- New: app/src/wearables/models.ts\n- app/src/lib/training/scheduler.ts (if needed for next action derivation)\n- app/src/lib/userSettings.ts (if a toggle is needed)\n\nDo-not-touch list:\n- app/src/components/** (no UI changes)\n- app/src/routing/\n\nStep-by-step edits:\n1) Define WearableGlanceModel types in wearables/models.ts with TrainingNextActionCard.\n2) Implement WearablesProjectionService that takes a DomainSnapshot and outputs TrainingNextActionCard using existing training data (read-only).\n3) Add logging tags [WEAR_PROJ] when projection runs.\n4) No platform delivery yet; do not add watchOS/Wear OS code.\n\nVerification checklist:\n- Run: resolved_commands.typecheck\nExpected: no type errors; projection types compile.\n\nRollback plan:\n- git revert <commit_sha>\n- Remove wearables folder.\n\nLogging tags to add:\n- [WEAR_PROJ]\n\nStop conditions:\n- If generating a snapshot requires new DB queries or schema changes, stop and document required inputs.\n","verification":["resolved_commands.typecheck"],"rollback":["git revert <commit_sha>","rm -rf app/src/wearables"],"risk_notes":["Low risk; additive types and projection only."]},{"phase":9,"name":"Wearables Phase 2 (expand cards + delivery adapters)","cursor_mode":"Plan","cursor_model_primary":"OpenAI GPT-5","cursor_model_fallback":"Anthropic Claude 4.5 Sonnet","why_model":"Requires deeper architectural planning for delivery adapters and OS constraints; a reasoning model is best.","cursor_prompt":"Goal: Expand wearable glance models and outline delivery adapters without duplicating domain logic.\n\nExact files to touch:\n- app/src/wearables/WearablesProjectionService.ts\n- app/src/wearables/models.ts\n- New: app/src/wearables/WearablesDeliveryService.ts (interfaces only)\n\nDo-not-touch list:\n- app/src/screens/\n- app/src/components/\n- app/src/routing/\n\nStep-by-step edits:\n1) Add RecoverySummaryCard and MoodCheckinCard to models.ts.\n2) Extend WearablesProjectionService to generate these from existing domain snapshot fields (no new queries).\n3) Add delivery interface with method stubs for Apple Watch and Wear OS adapters (no native code).\n4) Add [WEAR_PROJ] logs per card type.\n\nVerification checklist:\n- Run: resolved_commands.typecheck\nExpected: no type errors.\n\nRollback plan:\n- git revert <commit_sha>\n- Restore models and projection to Phase 1 only.\n\nLogging tags to add:\n- [WEAR_PROJ]\n\nStop conditions:\n- If platform-specific implementations are required, stop and document expected adapters separately.\n","verification":["resolved_commands.typecheck"],"rollback":["git revert <commit_sha>","git checkout -- app/src/wearables/models.ts app/src/wearables/WearablesProjectionService.ts"],"risk_notes":["Low risk; additive projection only."]}]}


File (1)
ok buddy, I want you to look at the dashboard  , I want you to compare it to the most preimuim apps and their dashboards, im not sure I dig it. It just looks bland in my opioinion and doesnt you know bring the app together. I want you to tell me if you agree with me, and what you think we could do to improve it and make it super preimuim and want users to check it out as offten as possible
Copy
Quick take: I mostly agree with your assessment
Based on the current Dashboard implementation, it’s a stacked-card layout with fairly uniform visual weight (ActionCard/InformationalCard blocks, similar padding, similar headers), which can read as flat and “functional” rather than premium or cohesive. The structure is solid, but the visual hierarchy isn’t strongly differentiated between the “hero,” primary action, and the rest of the feed (insight, sleep, progress, today, mood, recovery, celebrate).

Why it can feel bland (from the current code)
Uniform card weight & density
Most sections are rendered as similarly styled InformationalCard/ActionCard blocks with consistent spacing and headers, which flattens hierarchy and makes the screen feel like a checklist rather than a narrative. 

Hero/primary action not differentiated enough
The “Hero” and “Primary Next Action” are still card-like and visually similar to the rest of the feed, so they don’t read as premium focal points. 

Content is rich but not “stitched together”
You have multiple components (InsightCard, ProgressRing, ScheduleOverlay, CelebrateRow, FAB Group) but there’s no strong visual framing tying them into a single cohesive story for the day. 

What premium dashboards typically do better (and how to apply it here)
1) Make the hero a distinct, branded “moment”
Goal: A premium “today” hero that anchors the app’s identity.

Upgrade the hero into a larger, branded surface with stronger typography, gradient, or imagery; make the greeting + “last synced” + CTA feel like one intentional block rather than a normal card.

Currently it’s an ActionCard with a small icon and a “Sync” button, which reads as utility, not delight.

2) Create a “Today Score” or “Readiness Stack”
Goal: One compact, high-signal metric that becomes the daily reason to open the app.

You already compute “progressMetrics” and render ProgressRings; elevate one aggregated “today score” above the ring grid, and make the rings secondary. 

This creates a premium “single point of truth” moment that feels cohesive.

3) Turn the “Primary Next Action” into a premium CTA strip
Goal: The next best action should feel special and visually distinct.

Keep the data you already show (title/subtitle/meta), but push this into a wide CTA bar with stronger contrast and a clear icon treatment.

Right now it’s another ActionCard with a small icon and a button, so it blends in. 

4) Refine the “Insight” block into a feature card
Goal: Premium apps highlight a single, “editorial” insight.

You already have InsightCard and FeatureCardHeader.

Consider giving “Today’s insight” a unique visual treatment (accent color, subtle texture, progress to next insight) so it stands out from sleep/mood cards. 

5) Condense the feed into “Morning / Midday / Evening” rails
Goal: Premium dashboards feel like a narrative, not a grid of widgets.

Group the existing sections into time-based or theme-based rails:

Morning: sleep + readiness + today plan

Midday: insight + mood quick check-in

Evening: recovery + schedule

You already have the components; it’s about grouping and visual scaffolding. 

6) Introduce premium micro-interactions
Goal: Subtle motion makes a dashboard feel high-end.

You already use FAB + portals; add small transitions for card reveals or progress ring updates.

Make the CelebrateRow and progress rings animate with a subtle easing; it’s already there but could become a signature moment. 

7) Improve visual cadence by alternating card sizes
Goal: Avoid “stacked cards fatigue.”

Use a pattern: large feature card → compact row card → large card → split row.

You already have sections like Sleep, Progress, Today; mix their sizes to create rhythm. 

Concrete “premium” refresh blueprint using existing pieces
Hero: Expand the hero to be a brand surface (large greeting + daily status + CTA). Keep sync but make it a subtle secondary action. 

Primary Action: Upgrade to a full-width, high-contrast CTA strip with distinct styling. 

Insight: Feature-level styling (accent border, editorial typography) with one highlight per day. 

Progress: Add one “Today score” above the ring grid to unify metrics. 

Today Schedule: Keep, but present in a slimmer, modern timeline style. 

Mood + Recovery: Pull into a two-column or staggered layout to break the card stack. 

CelebrateRow: Keep, but give it a celebratory highlight state (sparkle/micro-animations). 

Bottom line
Yes, I agree the current dashboard can read as bland. It has a solid functional foundation, but it’s not visually “premium” because the hierarchy and narrative cadence are too uniform. By elevating the hero, tightening hierarchy, and adding a clear “today” story, it can feel far more premium and habit-forming without rebuilding core logic. 

Commands I ran
sed -n '1,240p' /workspace/Reclaim/app/src/screens/Dashboard.tsx

rg -n "FeatureCardHeader|InsightCard|ProgressRing|InformationalCard|ActionCard|ScheduleOverlay|CelebrateRow|FAB" /workspace/Reclaim/app/src/screens/Dashboard.tsx

nl -ba /workspace/Reclaim/app/src/screens/Dashboard.tsx | sed -n '1,200p'

nl -ba /workspace/Reclaim/app/src/screens/Dashboard.tsx | sed -n '1670,2300p'

Can you generate an image of how you think it would look with your suggestions?
Copy
Here’s a visual mockup of a more premium, cohesive dashboard concept based on the suggestions above:

Captured screenshot

If you want, I can translate this into concrete UI changes in your actual Dashboard.tsx (hero styling, primary CTA strip, daily score block, timeline styling, etc.) and provide a real in-app screenshot once implemented.

is that mobile friendly?
Copy
Yes — the design concept is intended to be mobile‑friendly, but the mockup I shared was a desktop‑sized visual to show hierarchy and layout. On mobile, the same structure collapses into a clean vertical flow:

Mobile-friendly layout translation
Hero becomes a full‑width top card with stacked text + a single CTA button.

Primary Next Action stays a single full‑width CTA strip (no side‑by‑side).

Insight becomes the next full‑width card, keeping the accent edge.

“Today’s Score” rings stack or use a horizontal scroll row.

Today Schedule stays full‑width with a simple timeline list.

Mood + Recovery stack vertically instead of 2‑column.

CelebrateRow becomes a horizontal carousel or a stacked trio.

Why it stays usable on mobile
The hierarchy improvements I suggested are primarily visual emphasis and spacing, not complex layouts. Everything can be implemented in a single column with adaptive spacing and card weights, which is standard for premium mobile apps.

If you want, I can generate a mobile-sized mockup (e.g., iPhone 14 Pro viewport) to show exactly how it would look on‑device.

user attachment image
user attachment image
hmmmm Dont know.....It doesnt look exciting enough. I kind of like these designs attached. What other catchy statistics based on the data we can get could we potentially add?
Copy
Below is a clear, design‑inspired response grounded in the actual data you already have on the Dashboard so we can add premium, catchy stats without inventing new pipelines.

✅ Do I agree it still feels bland vs those premium references?
Yes — and your references show what’s missing:

Strong visual rhythm (big “hero” + smaller stat tiles).

Mini‑charts / sparklines on summary tiles.

Prominent micro‑KPIs (percent change, “trend up/down”).
Right now your dashboard is mostly stacked cards with similar weight, so it doesn’t pop the way those premium UIs do.

🔥 Catchy premium stats you can add with the data you already have
Here are high‑impact KPI tiles that are 100% derivable from your existing dashboard data sources (sleep, mood, meds, streaks, recovery, insights, schedule).

1) “Recovery Momentum” (7‑day trend)
What it shows: % change in recovery stage metrics week‑over‑week (or “stage stability”).

Data source: getRecoveryProgress, getStageById already used on Dashboard.

2) “Sleep Quality Score” (with trend arrow)
What it shows: avg sleep duration + efficiency trend over 7 days.

You already fetch latest sleep session and show efficiency; build a score tile and trend.

Data source: listSleepSessions + sleep session mapping already in Dashboard.

3) “Consistency Streak” (sleep midpoint drift)
Catchy label: “Consistency +12% this week”

Data source: you already compute sleep midpoint deviation and show “Sleep consistency.”

4) “Adherence Momentum” (meds % + delta)
Catchy tile: “Meds +4% vs last week”

Data source: computeAdherence, listMedDoseLogsRemoteLastNDays. You already calculate med adherence and show “Meds: On track.”

5) “Mood Stability Index”
What it shows: 7‑day mood variance or “volatility.”

Mood quick check‑in exists; use recent mood logs to show stability trend.

Data source: mood check‑ins + addMoodCheckin already used on Dashboard.

6) “Daily Intent Completion Rate”
Catchy tile: “Today: 2/3 intentions completed”

Data source: routine suggestions and accept/skip already on Dashboard; add completion ratio. 

7) “Insight Impact”
Catchy tile: “3 helpful insights this week”

Data source: insights already in Dashboard; you could count feedback events / seen insights. 

8) “Schedule Clarity”
Catchy label: “Next 3 commitments resolved”

Data source: schedule items + calendar events already rendered. 

9) “Momentum Rings”
You already have ProgressRing tiles for sleep/mood/meds.

Make them premium by adding:

Sparkline under each ring

% change indicator

Data source: progressMetrics already rendered. 

✅ Why these feel “premium”
Premium dashboards feel alive because they show:
✔ trend indicators (up/down)
✔ compact KPIs (percent change, time saved, improvement)
✔ small charts inside tiles
You already have the data, just not the presentation layer.

🎯 If you want it to feel like your references
You can mimic that premium style with:

Top row of 3–4 “stat tiles” with small charts

One featured hero card

Colored trend chips (+4%, −2%)

Mini‑sparklines under each stat

All of that can be done using the data already on this screen. 

If you want, I can produce a new mobile mockup styled closer to those images and show exactly where these KPI tiles would go.

yes please generate 3 potential mockup versions with the stats but also with current cards unless you can creatively put them togeether or something. Mobile mockups please
Copy
Here are 3 mobile mockup options that keep your current cards but add premium stat tiles and higher visual rhythm:

1) Premium Flow (hero → action → insight → stats)
Captured screenshot

2) Glass + Stats Rail (softer + glass‑y panels)
Captured screenshot

3) Bold Tiles (more contrast + punchy tiles)
Captured screenshot

If you want, tell me which direction you like best (or mix‑and‑match), and I’ll translate it into concrete UI changes in Dashboard.tsx.