# Local agent / tooling scratch (never commit)
.tmp/
.tmp/**

# Node / Expo
# Local Expo prebuild output — commit only when maintaining a deliberate native baseline
app/android/
app/ios/

node_modules
app/node_modules
.expo
app/.expo
app/.expo-shared
dist
build
*.log
.DS_Store
.env
app/.env
app/app.json.bak
*.zip
app/reclaim.zip

# Reskin QA scratch captures (keep docs/reskin/final/ for handoff)
docs/reskin/qa-*.png
docs/reskin/qa-rerun-*.png
docs/reskin/phase5-*.png
docs/reskin/phase-0/*.png
app/nul
